import java.util.List;

/*
* A holder so we can work with all entities together
*/
public interface SpotifyEntity{

  //String getID();
  //List<SpotifyEntity> parseResponse(SpotifyResult result);

}
